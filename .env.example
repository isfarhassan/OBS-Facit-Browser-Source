# Faceit Open API (developer.faceit.com) — server key required for api/faceitstats
FACEIT_SERVER_API_KEY=
# Optional: alternate name; used if FACEIT_SERVER_API_KEY is unset
Server_Key=
# Optional: client key (not used by the current API route)
Client_Key=
